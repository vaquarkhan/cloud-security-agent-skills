Run multi-cloud security posture check (mock environment, no cloud credentials required).

Execute:
```
python examples/mock-posture-check/run_posture_check.py
python examples/mock-posture-check/run_posture_check.py --json
```

Summarize assessments per cloud and link to matching preset under presets/.
